import React from "react";

function AddNote() {
  return (
    <div className="note-form">
      <form action="">
        <input type="text" />
        <button>Add Note</button>
      </form>
    </div>
  );
}

export default AddNote;
